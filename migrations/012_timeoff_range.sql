-- Отпуск часто берут на неделю-две, а не на один день. Добавляем дату окончания:
-- date — первый день, date_end — последний (включительно). Пусто = один день.
-- Часовой перерыв (обед) остаётся одним днём с заполненным временем.
ALTER TABLE employee_time_off ADD COLUMN date_end TEXT;
